{
  "name": "test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite serve",
    "build:server": "vite build --ssr src/entry-server.js --outDir dist/server",
    "generate": "tsc --noEmit && vite build --ssrManifest --outDir dist/static && npm run build:server && node prerender"
  },
  "dependencies": {
    "vue": "^3.0.11",
    "vue-router": "4.0.5"
  },
  "devDependencies": {
    "@types/node": "14.14.22",
    "@vitejs/plugin-vue": "^1.2.1",
    "@vue/compiler-sfc": "^3.0.11",
    "@vue/server-renderer": "^3.0.11",
    "compression": "^1.7.4",
    "express": "^4.17.1",
    "serve-static": "^1.14.1",
    "typescript": "4.2.3",
    "vite": "2.1.5"
  },
  "browserslist": [
    "defaults",
    "not IE 11"
  ]
}
